type Query {
    getDevices: [Device]
}

type Device {
    type: String
    ieeeAddr: String
    nwkAddr: Int
    manufId: Int
    manufName: String
    powerSource: String
    modelId: String
    epList: [Int]
    status: String
    joinTime: Int
}
